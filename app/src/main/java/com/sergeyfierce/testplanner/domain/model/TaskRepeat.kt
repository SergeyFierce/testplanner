package com.sergeyfierce.testplanner.domain.model

enum class TaskRepeat {
    NONE,
    EVERY_DAY,
    WEEKDAYS,
    EVERY_WEEK,
    EVERY_TWO_WEEKS,
    EVERY_THREE_WEEKS,
    EVERY_MONTH,
    EVERY_YEAR,
    FLEXIBLE
}
